<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Resturant_management.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Resturant_management"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
		<TextBlock Text="Login" Style="{StaticResource TitleLargeTextBlockStyle}" Padding="50" HorizontalAlignment="Center"/>
		<TextBlock Text="Staff ID" Style="{StaticResource SubtitleTextBlockStyle}"/>
		<TextBox AutomationProperties.Name="multi-line TextBox" TextWrapping="Wrap" AcceptsReturn="True" IsSpellCheckEnabled="True" SelectionHighlightColor="Green" MinWidth="400" />
		<TextBlock Text="Password" Style="{StaticResource SubtitleTextBlockStyle}" Padding="0, 30, 20, 0"/>
		<TextBox AutomationProperties.Name="multi-line TextBox" TextWrapping="Wrap" AcceptsReturn="True" IsSpellCheckEnabled="True" SelectionHighlightColor="Green" MinWidth="400" />
		<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
			<Button x:Name="back" Margin="0, 30, 10, 0">Back</Button>
			<Button x:Name="login_btn" Margin="0, 30,0, 0">Sign In</Button>
		</StackPanel>
	</StackPanel>
</Page>
